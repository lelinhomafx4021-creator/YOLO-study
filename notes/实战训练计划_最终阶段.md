# 实战训练计划 — 最终阶段

## 当前状态

```
理论:   笔记 01-26 全部过完 ✅
代码:   已写了 CNN 训练、调参实验、导出脚本
缺实战: 保存/加载、模型诊断、视频检测、跟踪

目标:   跑完 6 个实战关卡 → 视觉算法岗项目经验包
```

---

## 关卡 1：完整训练→保存→加载→推理

**目标**：亲手验证 `model.state_dict()` 训练前后确实变了，保存后加载参数一模一样。

**脚本**：`00_basics/full_pipeline_save_load.py`

**跑法**：
```bash
python 00_basics/full_pipeline_save_load.py
```

**检查点**：
- [ ] 训练前 Conv1 weight 是随机数（0.0xxxx）
- [ ] 训练后同一层 weight 变成了完全不同的值
- [ ] `torch.equal` 验证：训练前后 weight 确实变了 ✓
- [ ] 保存 → 加载 → `torch.equal` 验证：加载后 = 训练后 ✓
- [ ] 推理 8 张图，大部分预测正确

**对应笔记**：笔记 17 + `API速查`

---

## 关卡 2：安全帽模型诊断

**目标**：用你训练的安全帽模型数据，一键出诊断报告。

**脚本**：`00_basics/10_error_analysis.py`

**跑法**：
```bash
python 00_basics/10_error_analysis.py
```

**检查点**：
- [ ] 4 张图正常弹出（Loss曲线 / P-R曲线 / mAP曲线 / 过拟合诊断）
- [ ] 终端打印诊断报告
- [ ] 回答：你的模型偏误检还是偏漏检？
- [ ] 回答：有没有过拟合迹象？（早期 gap vs 后期 gap）
- [ ] 回答：最佳 mAP 在哪一轮？最后一轮比最佳差多少？

**对应笔记**：笔记 10 + 10b + 12

---

## 关卡 3：NMS 可视化

**目标**：亲眼看到 NMS 阈值从 0.5 改成 0.8 后，框的数量怎么变。理解 Soft-NMS 和传统 NMS 的区别。

**脚本**：`00_basics/10_nms_demo.py`

**跑法**：
```bash
python 00_basics/10_nms_demo.py
```

**检查点**：
- [ ] 第一张图（NMS 前）→ 8 个框全在
- [ ] 第二张图（NMS IoU=0.5）→ 删了几个？
- [ ] 第三张图（Soft-NMS）→ 和传统 NMS 对比，多保留了哪个框？为什么？
- [ ] 第四张图（分数对比）→ 哪两个框分数被 Soft-NMS 降了但还活着？

**对应笔记**：笔记 11 拷问二 + `10_nms_demo.py` 代码注解

---

## 关卡 4：ONNX 推理 + 内存对比

**目标**：在你自己电脑上，跑 `.pt` 和 `.onnx` 推理，亲眼看到内存和速度差距。

**前置**：
```bash
# 确认有 ONNX 模型
ls runs/safety_helmet/yolo11n_baseline_v1/weights/best.onnx

# 如果没有，先导出：
python 02_export_deploy/01_export.py

# 确认安装了 onnxruntime
pip install onnxruntime-gpu
```

**跑法**：新建 `00_basics/test_onnx_vs_pt.py`

```python
# 拷贝笔记 22 第三节的代码，或者直接跑：
python -c "
import time, os, psutil, onnxruntime as ort, numpy as np, cv2
from ultralytics import YOLO
import torch

process = psutil.Process(os.getpid())
# 笔记 22 的完整代码
"
```

**检查点**：
- [ ] `.pt` 推理一次多少 ms？内存涨了多少 MB？
- [ ] `.onnx` 推理一次多少 ms？内存涨了多少 MB？
- [ ] 内存差距是几倍？（通常 3-5 倍）
- [ ] 速度差距是多少？

**对应笔记**：笔记 18 + 22

---

## 关卡 5：视频检测实战（单线程跳帧）

**目标**：拿一段视频（或者摄像头），跑实时检测。验证跳帧之后流畅度提升多少。

**前置**：准备一段测试视频（或者用摄像头 `source=0`）

**脚本**：`00_basics/11_video_detection_skipframe.py`

**跑法**：
```bash
python 00_basics/11_video_detection_skipframe.py
```

**检查点**：
- [ ] 不跳帧 → FPS 多少？画面卡不卡？
- [ ] 每 3 帧推理一次 → FPS 提升到多少？
- [ ] 每 5 帧推理一次 → FPS 多少？框有明显的延迟感吗？
- [ ] 找到你的设备上"流畅度 vs 精度的最佳平衡点"（跳几帧最好）

**对应笔记**：笔记 20 + 24

---

## 关卡 6（加分项）：ByteTrack 多目标跟踪

**目标**：给 YOLO 加上跟踪，每个安全帽/人持续带着 ID，能统计"这个人没戴安全帽在危险区待了多久"。

**前置**：
```bash
# YOLO 自带 ByteTrack，不需要额外安装
```

**脚本**：`00_basics/12_bytetrack_tracking.py`

**跑法**：
```bash
python 00_basics/12_bytetrack_tracking.py
```

**检查点**：
- [ ] 画面里每个人/安全帽有独立 ID
- [ ] 同一个人移动，ID 不变
- [ ] 人走出画面再回来 → ID 变了吗？（变了也正常，ByteTrack 不是 DeepSORT）
- [ ] 面试话术：能说出 ByteTrack 和 SORT/DeepSORT 的区别

**对应笔记**：笔记 23 的运动预测部分

---

## 时间预估

```
关卡 1：训练→保存→加载      约 10 分钟（跑脚本 + 看输出）
关卡 2：模型诊断             约 5 分钟（跑完看报告）
关卡 3：NMS 可视化           约 5 分钟（跑完看 4 张图）
关卡 4：ONNX 内存对比         约 15 分钟（导出 ONNX + 跑代码对比）
关卡 5：视频检测             约 20 分钟（找视频 + 调跳帧参数）
关卡 6：ByteTrack 跟踪        约 15 分钟（附加分）
─────────────────────────────────────
总计                       约 70 分钟
```

## 跑完后的状态

```
✅ 知道模型参数怎么保存、怎么加载、怎么验证
✅ 能看懂训练日志、诊断过拟合/欠拟合/误检/漏检
✅ 理解 NMS 为什么在密集场景误杀，知道 Soft-NMS 怎么解决
✅ 亲手对比了 ONNX 和 PyTorch 推理的内存/速度差距
✅ 视频实时检测跑通了，知道跳帧和双线程的区别
✅ 目标跟踪跑通了，知道卡尔曼滤波在跟踪里的作用

→ 拿这 6 个关卡的经验写简历，视觉算法基础岗够了
```
