# 当前状态

更新时间：`2026-05-20`

## ✅ 2026-06-15 进度记录

- 12 关实战代码已通关，主线已经从“跑通”进入“复盘和固化”阶段。
- 当前可以直接复习的内容是：`00_basics/` 的 PyTorch / CNN / NMS / 调参 / 视频推理，以及 `01_helmet_detect/` 的安全帽检测 baseline。
- 现在的重点不是继续铺新知识点，而是把每个笔记末尾的问题和答案对齐，形成可直接背诵的复习材料。
- 下一步优先做两件事：整理笔记答案、继续看安全帽项目的 `val` 结果和 bad case。

## ✅ 已完成

- **基础环境配置**：
  - Python 3.11.9（uv 管理）
  - PyTorch 2.7.1+cu118（CUDA 可用，RTX 3050 Laptop GPU）
  - OpenCV 4.13.0
  - Ultralytics >= 8.4.51
- **Week 1 OpenCV 基础**：
  - 学习并编写了图像读取、缩放、裁剪、颜色转换和视频帧处理。
  - 封装了用于可视化检测框的 `draw_detection` 函数。
  - 核心脚本在 `week1_opencv/` 下，已全部验证通过。
- **Week 2 PyTorch 基础**：
  - 掌握了 `Dataset`、`DataLoader` 的核心写法。
  - 手写了一个二分类模型 `SimpleModel`，打通了经典训练循环（前向传播、Loss计算、梯度清零、反向传播、权重更新）。
  - 对比了底层 PyTorch 和高层框架（YOLO）的运作机制，整理了详细的 `pytorch_notes/` 概念笔记。
- **Week 3 YOLO 目标检测基础**：
  - 实现了 `01_yolo_predict.py` 基础推理。
  - 编写了 `05_optimized_train.py` 和 `08_multtest.py`，使用 `coco8` 和 `coco128` 数据集跑通了 YOLO 的完整训练和评估流程。
  - 进行了多次调参实验（在 `runs/detect/` 下生成了多次训练记录），学会了根据 Precision、Recall、mAP 等指标进行数据增强和超参数调优。

## ⏳ 进行中 / 尚未开始

- **寻找自定义项目数据集（当前核心任务）**：
  - 需要脱离官方的 `coco8`/`coco128` 玩具数据集，选定一个能够写进简历的真实目标检测业务场景（如：安全帽检测、口罩检测等），并获取其 YOLO 格式的数据集。
- **Week 4 微调实战（待开始）**：
  - 导入自定义数据集。
  - 进行模型微调训练。
  - 进行误检和漏检分析，记录调参过程。
- **Week 5 姿态估计 Demo（待开始）**：
  - 跑通 YOLO Pose 推理，作为项目加分项。
- **Week 6 导出与部署（待开始）**：
  - 模型 ONNX 导出，整理出一套面试能讲顺的项目表达。

## 🎯 当前方向共识

- **实战项目导向**：坚决不卷纯理论，以“能跑通、能看懂指标、能讲出调参过程”为第一标准。
- **简历包装核心**：后续的微调一定要基于一个有现实意义的“自定义数据集”，为面试准备素材。
